# DEMO QA — کارخانه لنت ترمز آریاترمز

Routes and actions mirror interactive demo quality baseline for آریاترمز.

Primary routes: `/` `/plan` `/intelligence` `/work` `/map` `/agents` `/communication` `/records/:type/:id`

Dead controls target: 0 for primary/secondary CTAs.

Deliberately limited: voice input opens Ask Steve; paperclip inserts link marker.
